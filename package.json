{
  "name": "subgraph-registry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ maximehg/subgraph-registry",
    "create-local": "graph create --node http://localhost:8020/ maximehg/subgraph-registry",
    "remove-local": "graph remove --node http://localhost:8020/ maximehg/subgraph-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 maximehg/subgraph-registry"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.0",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
